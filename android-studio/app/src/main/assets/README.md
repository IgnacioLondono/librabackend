# Assets Directory

Coloca aquí el archivo `seed-data-for-microservices.json` con los datos iniciales.

El archivo debe contener:
- Array de `books` con 34 libros
- Array de `users` con 2 usuarios

La estructura del JSON debe ser exactamente como se muestra en el prompt de Cursor.







